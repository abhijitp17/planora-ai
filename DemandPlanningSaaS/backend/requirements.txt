fastapi>=0.100.0
uvicorn>=0.23.0
pandas>=2.0.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
xgboost>=2.0.0
lightgbm>=4.0.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
python-multipart>=0.0.6
psycopg2-binary>=2.9.0
python-jose[cryptography]>=3.3.0
# NOTE: bcrypt is used directly (not via passlib). passlib 1.7.4 (its last release,
# 2020) is incompatible with bcrypt >= 4.1 — its backend self-test feeds bcrypt a
# >72-byte probe that modern bcrypt rejects, breaking hashing entirely. bcrypt-direct
# is the maintained, future-proof choice and fully satisfies the "bcrypt hashing" requirement.
bcrypt>=4.0.0
python-dotenv>=1.0.0
networkx>=3.0
scipy>=1.10.0
redis>=5.0.0
scikit-optimize>=0.9.0

# Database migrations (Phase 0 — Alembic owns schema; no implicit create_all in prod)
alembic>=1.13.0

# Testing (Phase 0 — pytest suite + FastAPI TestClient transport)
pytest>=8.0.0
httpx>=0.27.0
